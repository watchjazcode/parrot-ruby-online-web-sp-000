# Create method `parrot` that outputs a given phrase and
# returns the phrase

def parrot(phrase= "Squawk!") #defining method with a default argument
  puts "#{phrase}" #i'm outputting the argument
  return phrase #i'm returning the argument #test says "pretty bird, that is how it knows"
end
